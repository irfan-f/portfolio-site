/** Canonical production origin. */
export const SITE_URL = 'https://irfan-f.com';

/** Source repo for this portfolio (irfan-f.com). */
export const SITE_GITHUB_URL = 'https://github.com/irfan-f/portfolio-site';

/** Tooltip / `title` — may name GitHub explicitly. */
export const SITE_FEEDBACK_TITLE =
  'Report feedback about this website on GitHub (opens in a new tab — choose bug, feature, or task)';

/**
 * `aria-label` for the site feedback link — avoid the substring “GitHub” so it
 * does not collide with social profile links whose accessible name is “GitHub”.
 */
export const SITE_FEEDBACK_ARIA_LABEL =
  'Report feedback or suggest a change for this website (opens issue template in a new tab)';
